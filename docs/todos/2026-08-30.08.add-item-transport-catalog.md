# Add Item Transport Catalog

Status: Complete

## Context

Logical wiring identifies an item and rate, while facility ports already identify fixed direction and transport kind. Routing needs an external item-to-transport mapping so belt versus pipe is validated input rather than a solver choice.

## Completion Criteria

- Add a strict external item catalog schema.
- Validate schema version, stable item IDs, duplicate IDs, and unknown fields.
- Promote valid catalogs to an indexed validated type.
- Share one transport kind type between items and facility ports.
- Add a CLI validation command and example data.
- Document the flattened facility-mode boundary.
- Pass formatting, workspace checks, tests, Clippy, and manual CLI validation.

## Activity Log

- 2026-08-30: Added item catalog schema version 1 with fixed belt-or-pipe transport kinds.
- 2026-08-30: Replaced the facility-only port transport enum with shared `TransportKind`.
- 2026-08-30: Recorded flattened mode-specific facilities as the preprocessing boundary.
- 2026-08-30: Added CLI validation and verified the example item catalog.
