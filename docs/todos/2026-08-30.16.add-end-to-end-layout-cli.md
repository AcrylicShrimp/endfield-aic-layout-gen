# Add End-to-End Layout CLI

Status: Complete

## Context

The implemented stages could be invoked separately, but no command accepted all runtime contracts and returned a routed layout.

## Completion Criteria

- Add an explicit integrated layout subcommand.
- Execute throughput, facility, wiring, catalog, and spatial stages in order.
- Require recipe, request, facility, item, and hard-bound input paths.
- Return the first structured stage failure without continuing.
- Add a positive solver time-limit flag with a safe default.
- Return a feasible incumbent when optimality is not proven before the limit.
- Avoid posting empty endpoint constraints to Pumpkin.
- Manually verify the complete example pipeline.
- Pass formatting, workspace checks, tests, and Clippy.

## Activity Log

- 2026-08-30: Added `aic layouts solve` and explicit runtime input flags.
- 2026-08-30: Added timed integrated solve support with a ten-second CLI default.
- 2026-08-30: Fixed empty endpoint-cell constraint posting found by the first full run.
- 2026-08-30: Produced a feasible three-facility, four-route layout in a two-second run.
